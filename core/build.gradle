// core: 순수 도메인 모듈
// 공통 의존성(spring-boot-starter, lombok, test)은 root build.gradle의 subprojects에서 상속

dependencies {
    implementation 'org.springframework:spring-tx'
}

tasks.register("prepareKotlinBuildScriptModel"){}
